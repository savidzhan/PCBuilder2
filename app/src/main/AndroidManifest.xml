<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.savijan.pcbuilder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PCBuilder">
        <activity android:name=".drawer_main_activity" android:theme="@style/Theme.PCBuilder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ComponentAddActivity" android:theme="@style/Theme.PCBuilder.NoActionBar"/>
        <activity android:name=".ViewListActivity" android:theme="@style/Theme.PCBuilder.NoActionBar"/>
        <activity android:name=".CategoryActivity" android:theme="@style/Theme.PCBuilder.NoActionBar"/>
        <activity android:name=".HomeActivity" android:theme="@style/Theme.PCBuilder.NoActionBar"/>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.PCBuilder.NoActionBar"/>
        <activity android:name=".LoginActivity" android:theme="@style/Theme.PCBuilder.NoActionBar"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.PCBuilder.NoActionBar">

        </activity>
    </application>

</manifest>